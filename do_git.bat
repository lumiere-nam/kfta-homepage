git add shop_*.html
git commit -m "fix: entirely rewrite cloned detail pages to match their specific product titles, images, hooking texts, and usage instructions instead of duplicating body silhouette text"
git push
